import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Suspense } from "react";
import ScollTop from "./(components)/ScrollTop/ScrollTop";
import Header from "./(components)/Header/Header";
import Footer from "./(components)/Footer/Footer";
import Loading from "./(components)/Loading/Loading";
import "./globals.css";
import AdminProvider from "@/common/components/AdminPropvier/AdminProvider";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "농행동행",
  verification: {
    google: "KJtnNooV2vUDrqgH9qXffo3DRefAFZPeH2XchmOKWN4",
    other: {
      "naver-site-verification": "9e15352754e06501dfa9e8e3cba85fc97e2d51bb",
    },
  },
  description: "Generated by create next app",
  icons: {
    icon: "/icon/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ScollTop />
        <AdminProvider>{children}</AdminProvider>
      </body>
    </html>
  );
}
